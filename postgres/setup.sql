CREATE DATABASE dev;
\connect dev;
CREATE TABLE urls (
   id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
   url VARCHAR(255) NOT NULL
);

CREATE DATABASE test;
\connect test;
CREATE TABLE urls (
   id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
   url VARCHAR(255) NOT NULL
);
